{{!-- <div class="container mt-5">
    <div class="d-flex justify-content-between my-5">
        <input id="myInput" type="text" placeholder="Search..">
        <a href="/admin/add-coupon" class="btn btn-success">Add Coupon</a>
    </div>

    <table class="table">
        <thead>
            <tr>
                <th scope="col">Coupon Name</th>
                <th scope="col">Coupon Code</th>
                <th scope="col">limit</th>
                <th scope="col">Discount</th>
                <th scope="col">Expiry Date</th>
                <th scope="col">Actions</th>
            </tr>
        </thead>
        <tbody id="myTable">
            {{#each coupon}}
            <tr>
                <td><strong>{{this.name}}</strong></td>
                <td style="font-size:12 px">
                    <strong>{{this.couponCode}}</strong>
                </td>
                </td>
                <td>{{this.limit}}</td>
                <td>{{this.offer}}</td>
                <td>{{this.expirydate}}</td>

                <td>
                    <a href="/admin/deleteCoupons/{{this._id}}"
                        onclick="return confirm('Are you suren to delete coupon: {{this.couponName}}?')"
                        class="btn btn-danger">Delete</a>


                </td>
            </tr>
            {{/each}}



        </tbody>


    </table> --}}

    <section>
    <div class="container"> 
        <div class="">
            <a  href="/admin/add-coupon" class="btn btn-success mt-5">Add coupon</a>
        </div>
        <table class="table mt-5 display" id="productTable">
            <thead>
                <tr>
                    
                                 <th scope="col">SL-NO:</th>
                                <th scope="col">Coupon</th>
                                <th scope="col">Offer in %</th>
                                <th scope="col">Validity</th>
                                <th scope="col">Action</th>
                </tr>
            </thead>
            <tbody>
                {{#each coupon}}
                <tr>
                                <th sope="row">{{inc @index}}</th>
                                <td>{{this.name}}</td>
                                <td>{{this.offer}}</td>
                                <td>{{this.validity}}</td>
               <td>                           
                      {{!-- <a href="/admin/editproduct/{{this._id}}" class="btn btn-primary">Edit</a> --}}
                      <a href="/admin/delete-coupon/{{this._id}}" class="btn btn-danger"
                      onclick="return confirm('Are you sure to Delete {{this.name}}?')">Delete</a>
                     </td>
                </tr>

                                         
                {{/each}}
            </tbody>
        </table>
    </div>

</section>

{{!--  --}}

<script>
    $(document).ready(function () {


        $('#productTable').wrap('<div id="hide" style="display:none"/>');

        $('#hide').css('display', 'block');
    });


    var table = $('#productTable').DataTable();
</script>